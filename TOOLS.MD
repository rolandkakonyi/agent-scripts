# TOOLS.MD

Tool list now lives in `AGENTS.MD` inside the `<tools></tools>` block. This stub remains for compatibility with older instructions.

READ ~/Projects/agent-scripts/{AGENTS.MD,TOOLS.MD} BEFORE ANYTHING (skip if files missing).
